/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: 'class', // Enables class-based dark mode
    content: ['./*.html'],
    theme: {
        extend: {},
    },
    plugins: [],
};
